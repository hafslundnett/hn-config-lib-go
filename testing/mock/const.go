package mock

// testing constants, used for consistency across tests
const (
	ID    = "mockID"
	Role  = "mock_role"
	Token = "mock-token"
	Addr  = "mock.addr"
	Path  = "mock/path"
	URL   = "https://" + Addr + "/" + Path
	File  = "mock.file"
)
